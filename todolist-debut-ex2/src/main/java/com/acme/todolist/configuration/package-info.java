/**
 * Entités du domaine (au sens DDD, pas JPA)
 * 
 * En général, classes complexes (méthodes riches, relations entre les entités,  pas de simples POJO anémiques)
 */
package com.acme.todolist.configuration;